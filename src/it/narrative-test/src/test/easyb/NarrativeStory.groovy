narrative "description", {
    as_a "role"
    i_want "something"
    so_that "something else"
}

scenario "stack", {
    given "an empty stack"
    when "null is pushed"
    then "then the stack should still be empty"
}
